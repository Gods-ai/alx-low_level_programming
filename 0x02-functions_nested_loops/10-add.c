#include "main.h"
/**
 * add - prints the result of two integers
 *@one: where first number will be inputed
 *@two: where second number will be inputed
 * Return: Always 0
 */

int add(int one, int two)
{
	return (one + two);
}
