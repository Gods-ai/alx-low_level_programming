#include "main.h"

/**
 * mul - mutiplies two integers
 * @a: represent integer value
 * @b: rrepresents b integer value
 *
 * Return: Always the 0 (success)
 */

int mul(int a, int b)
{
	/* declarung a variable to store the result */
	int result = 0;

	result = a * b;

	return (result);
}
